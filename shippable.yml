language: node_js

node_js:
  - 5.10

env:
  global:
    - PROJECT=${REPO_NAME}

build:
  ci:
    - docker build -t ${PROJECT}:ci .
  post_ci:
    - . docker-push.sh

integrations:
  hub:
    - integrationName: "Docker Hub"
      type: docker